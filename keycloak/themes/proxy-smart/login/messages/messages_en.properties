# Theme message overrides. Keycloak loads the English bundle first and overlays
# the requested locale, so a key defined only here renders in English elsewhere
# rather than leaking the key name.

# Label on the disclosure that reveals the username/password form, under the
# identity providers (see login.ftl).
passwordSignInToggle=Or sign in with your password
